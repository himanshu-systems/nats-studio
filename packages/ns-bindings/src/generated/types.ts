/*
 * Generated by typeshare from crates/ns-types. Do not edit by hand.
 * Regenerate with `pnpm gen:types` (which runs `cargo xtask gen-types`).
 * CI fails on drift between this file and the Rust source of truth.
 */

/** Stable, machine-actionable error codes shared verbatim with the frontend. */
export type ErrorCode =
  | "CONNECTION_TIMEOUT"
  | "CONNECTION_CLOSED"
  | "AUTH_FAILED"
  | "TLS_ERROR"
  | "PERMISSION_DENIED"
  | "REQUEST_TIMEOUT"
  | "NO_RESPONDERS"
  | "CANCELLED"
  | "TIMEOUT"
  | "SERIALIZATION"
  | "IO"
  | "NOT_FOUND"
  | "INVALID_ARGUMENT"
  | "INTERNAL";

/** The single wire error DTO delivered to the frontend for every failed command. */
export interface IpcError {
  code: ErrorCode;
  message: string;
  retriable: boolean;
  correlationId?: string;
  causes: string[];
}

/** Returned by the `app_info` command. */
export interface AppInfo {
  version: string;
  appSchemaVersion: number;
  pluginApiVersion: string;
  os: string;
  arch: string;
  buildChannel: string;
}
